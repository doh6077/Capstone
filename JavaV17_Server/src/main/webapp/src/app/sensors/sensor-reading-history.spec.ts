import { SensorReadingHistory } from './sensor-reading-history';

describe('SensorReadingHistory', () => {
  it('should create an instance', () => {
    expect(new SensorReadingHistory()).toBeTruthy();
  });
});
